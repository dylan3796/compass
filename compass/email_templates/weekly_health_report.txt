Subject: [Compass] Weekly Agent P&L — {{week_of}}

VERITAS AI — WEEKLY AGENT P&L
{{week_start}} – {{week_end}}

(The block below is generated by core/pnl_generator.render_text(conn) —
 every agent as a line item. This template wraps it with fleet context.)

{{pnl_table}}

PROMISED VS. DELIVERED
{{#each plan_misses}}
  ⚠ {{name}} is delivering {{plan_pct}}% of its projection ({{projection_source}})
    Diagnosis and fix in the dashboard.
{{/each}}
{{#each plan_beats}}
  ✓ {{name}} is beating its projection ({{plan_pct}}% of plan)
{{/each}}

FLEET AT A GLANCE
  {{n_healthy}} healthy · {{n_attention}} needs attention · {{n_critical}} critical · {{n_new}} new
  Cost this week:         ${{cost_week}}
  Value delivered:        ${{value_week}}
  Potential savings:      ${{savings_pending}}/mo ({{n_recs}} pending recommendations)

TOP RECOMMENDATION
  ▸ {{top_rec_agent}}: {{top_rec_description}}
    {{#if top_rec_savings}}est. savings: ${{top_rec_savings}}/mo — one click in the dashboard{{/if}}

WHAT CHANGED THIS WEEK
  {{n_applied}} recommendations applied · {{n_dismissed}} dismissed
{{#each changes}}
  ▸ {{agent}}: {{description}} — {{outcome}}
{{/each}}

Value = completed runs x each agent's documented unit value. Agents without
an honest value model show "—" — their cost and trend are still tracked.

— Compass · the management layer for the AI workforce
Open the dashboard for diagnoses and one-click fixes: {{dashboard_url}}
You're receiving this because you operate the Veritas AI agent fleet.
