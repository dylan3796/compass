/**
 * Engine version, stamped into every replay record. A verdict is replayable
 * as (records hash, config hash, engine version) — bump on any change that
 * can move a number.
 */
export const ENGINE_VERSION = "0.1.0";
